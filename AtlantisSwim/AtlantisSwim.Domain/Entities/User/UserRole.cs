namespace AtlantisSwim.Domain.Entities.User
{
    /// <summary>
    /// Standardised role values used across the platform.
    /// 1 = Student (Elev), 2 = Coach (Antrenor), 3 = Admin
    /// </summary>
    public enum UserRole
    {
        Student = 1,
        Coach   = 2,
        Admin   = 3
    }
}
